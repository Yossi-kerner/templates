version: 2
deploy:
  steps:
    setupVariables:
      after:
        - name: "Block network"
          run: | 
            echo 127.0.0.1 www.openpolicyagent.org >> /etc/hosts
            cat /etc/hosts
